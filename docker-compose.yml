version: '3.8'

services:
  frontend:
    build:
      context: . # Builds from the directory containing the Dockerfile
    ports:
      - "3000:80" # Map container port 80 to host port 3000
    environment:
      - NODE_ENV=production
    
